/*
import React from 'react'
import { renderRoutes } from '../../routeUtils'

const Home = props => {
    const { route } = props
    return (
    <div>
            {renderRoutes(route.routes)}
            </div>
    )
}

export default Home